# binaries / tools
/bin/
/dist/
# `go build ./cmd/<svc>` 在 repo 根目錄產出的 binary（正式產物走 make build → /bin/）
/api-gateway
/merchant-service
/payment-service
/ledger-service
/webhook-service
/reconciliation-service
/provider-mock
/provider-stripe
/.ci-out/
*.exe
*.test
*.out
coverage*.txt

# env / secrets
.env
.env.*
!.env.example
deploy/compose/.env

# IDE / OS
.idea/
.vscode/
.DS_Store

# go
vendor/
